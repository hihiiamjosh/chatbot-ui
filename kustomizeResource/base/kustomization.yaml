apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: auto-agent-ui

generatorOptions:
  disableNameSuffixHash: true

resources:
  - namespace.yaml
  - auto-agent-ui.yaml
  - service.yaml
  - ingress.yaml
